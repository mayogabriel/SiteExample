<?php

use Illuminate\Database\Seeder;

class CustomFieldTypeSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        /*$types = array(
                        'Text',
                        'Password',
                        'Hidden',
                        'Textarea',
                        'Select',
                        'Checkbox',
                        'Footer'
        );
        
        foreach ($types as $type){
            DB::insert('insert into custom_fields_types (name) values (?)', [$type]);
        }*/
        
    }
}
